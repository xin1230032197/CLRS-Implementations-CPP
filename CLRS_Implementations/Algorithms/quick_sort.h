#pragma once
#include <vector>

namespace clrs {
    namespace algo {
        void quick_sort_recursive(std::vector<int>& a, int zuo, int you);
        void quick_sort(std::vector<int>& a);
    } // namespace algo
} // namespace clrs